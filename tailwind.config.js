const colors = require('tailwindcss/colors');

module.exports = {
  purge: [],
  darkMode: false,
  theme: {
    extend: {
      colors: {
        indigo: colors.indigo,
        blueGray: colors.blueGray,
      },
    },
  },
  variants: {},
  plugins: [],
};
